// Create a file named `supabase.env.js` in the same folder when developing locally
// and add it to your local .gitignore. Do NOT commit secrets to the repository.

module.exports = {
  // Replace with your project's values for local development only.
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-anon-key-here'
};
